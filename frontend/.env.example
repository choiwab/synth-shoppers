# H1 dashboard env. Copy to .env.local to override.
VITE_API_BASE=http://localhost:8000
VITE_WS_BASE=ws://localhost:8000
# Force the fixture player (no backend needed). 1 = on.
VITE_MOCK=1
# Shopee storefront embedded inside each Agent Preview monitor.
VITE_SHOPEE_BASE=http://localhost:5174
VITE_SHOPEE_LISTING_ID=matin_kim_beanie_sg
